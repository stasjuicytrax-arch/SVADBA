# DESIGN.md — «В титрах» (v2)

Дизайн-система нового сайта. Источники: 3 референса в `Referens/` + палитра старого сайта help-wedding.ru.
**Главный референс по цвету и композиции — `Referens/1.jpg` (Reload, «Creative branding agency»).**

Настроение одной фразой: **тёмный кинозал, красный бархат, свет прожектора, финальные титры.**
Минимализм + сочность: много воздуха, крупный плотный гротеск, один насыщенный красный, живые фото.

---

## 1. Что берём из референсов

| Реф | Что забираем | Чего НЕ берём |
|---|---|---|
| **1.jpg — Reload** (главный) | Палитра oxblood → красный → огненно-оранжевый. Тёмный «зал» по краям, а контент — на светлом скруглённом «экране». Шкала-линейка с засечками над экраном (у нас = таймкод плёнки). Огромные заголовки капсом, плотный трекинг, **двухцветные строки** (бордовый + приглушённый розово-серый). **Фото-«пилюли» внутри заголовка.** Красная звёздочка ✱ как фирменный знак. Стеклянные мини-карточки поверх hero-фото («85%»). Карусель карточек услуг, центральная — приподнята. Pill-кнопки с обводкой и стрелкой ↗. | Мужские fashion-портреты, английский текст. |
| **2.jpg — NeoVision** | Дисциплина сетки и воздух. Тёмные секции с карточками-«стеклом» (отзывы: тёмная плашка + текст + аватар-кружок). Тонкие теги-пилюли. Блок «сплит»: фото слева, текст справа. Крупный финальный CTA-баннер с фото. Плотный многоколоночный футер. | Холодный хай-тек, серый монохром. |
| **3.jpg — Formora** | Гигантский вордмарк на всю ширину, где одна буква заменена символом (у нас: `В Т✱ТРАХ` или логотип с красной «и»). Строка-подпись из 3 фраз через тонкие линии над заголовком. **Стат-строка** «75% / 25% / 90 мин» с тонкими вертикальными разделителями. Тёплый градиент-«свечение» в фото-блоке. Смена светлой и тёмной плашки. | Оранжевый как основной цвет (у нас он только в свечении). |

---

## 2. Палитра (снята пипеткой с `Referens/1.jpg` + красный старого сайта)

```css
:root {
  /* Тёмный кинозал */
  --night:      #1B0100;  /* основной тёмный фон (референс 1, внешнее поле) */
  --night-deep: #0B0302;  /* самые глубокие тени, футер */
  --wine:       #360305;  /* тёмно-винный: тёмные плашки, карточки на тёмном */
  --wine-2:     #4B120D;  /* винный светлее: ховеры, бордеры на тёмном */

  /* Экран */
  --screen:     #F0EEEC;  /* светлый «экран» (реф 1 — #F0F0F0, чуть потеплили) */
  --screen-2:   #F8F8F2;  /* кремовый со старого сайта — альтернативные светлые секции */

  /* Текст */
  --ink:        #1A0503;  /* основной текст на светлом */
  --maroon:     #3E0204;  /* заголовки на светлом (реф 1) */
  --rose-mute:  #A39491;  /* «вторая строка» двухцветных заголовков, подписи */
  --rose-soft:  #C48F86;  /* приглушённый розовый: второстепенный акцент на тёмном */
  --paper:      #F7F2EC;  /* текст на тёмном */

  /* Акценты */
  --red:        #EB0F03;  /* сигнальный красный: ✱, активные состояния, главная CTA */
  --red-brand:  #CA2615;  /* фирменный красный старого сайта: бегущая строка, плашки */
  --ember:      #FB5D06;  /* огненно-оранжевый — ТОЛЬКО в градиентах/свечении */
  --ember-2:    #F44613;

  /* Градиенты */
  --glow-hero:  radial-gradient(120% 90% at 70% 20%, #FB5D06 0%, #CA2615 28%, #5B000C 60%, #1B0100 100%);
  --glow-edge:  radial-gradient(80% 60% at 50% 110%, rgba(202,38,21,.45), transparent 70%);
  --line-dark:  rgba(247,242,236,.14);
  --line-light: rgba(62,2,4,.14);
}
```

Правила:
- Страница «живёт» на `--night`. Контентные секции — это скруглённые светлые «экраны» `--screen` (radius 28–40px) с полями 12–24px от краёв окна, как в референсе 1. Между ними — тёмные «межкадровые» секции.
- Красный — **акцент, а не заливка**: ✱, одна главная кнопка, бегущая строка, активная точка. Большие плоскости красного — только через фото (hero-занавес) или градиент `--glow-hero`.
- Оранжевый `--ember` никогда не используется для текста или как плоская заливка.
- Никакого бежевого «AI beige», никаких фиолетово-синих градиентов.

---

## 3. Типографика

Нужна кириллица. Все шрифты — **локально** в `fonts/` (woff2, `font-display: swap`), только нужные начертания. Google Fonts не подключать (закон о ПДн, см. `docs/LEGAL.md`).

| Роль | Шрифт | Как |
|---|---|---|
| Display (заголовки) | **Inter Tight** 500/600 | UPPERCASE, `letter-spacing: -0.045em`, `line-height: .92`. Это ближе всего к плотному гротеску референса 1. |
| Текст | **Onest** 400/500 | 16–18px, `line-height: 1.55`, длина строки ≤ 62ch |
| Служебный / «титры» | **JetBrains Mono** 400 | 11–13px, UPPERCASE, `letter-spacing: .12em`: «КАДР 001», таймкоды, eyebrow-подписи, номера этапов |
| Логотип | готовый PNG `assets/sections/01-hero/02-photo.png` (белый) — позже перерисовать в SVG | |

Шкала (fluid):
```
--fs-mega:  clamp(3.5rem, 13vw, 12rem);   /* вордмарк «В ТИТРАХ» */
--fs-h1:    clamp(2.6rem, 7vw, 6.5rem);
--fs-h2:    clamp(2rem, 5vw, 4.25rem);
--fs-h3:    clamp(1.25rem, 2vw, 1.75rem);
--fs-body:  clamp(1rem, 1.1vw, 1.125rem);
--fs-meta:  0.75rem;
```

Приёмы заголовков (обязательно, это «подпись» стиля):
1. **Двухцветная строка** — часть фразы `--maroon`, часть `--rose-mute` (на тёмном: `--paper` + `--rose-soft`).
2. **Фото-пилюля в строке** — маленькое фото (высота = 0.8em, radius 999px) вставлено между словами заголовка, как в реф. 1. Внутри пилюли — медленный зум при скролле.
3. **Лесенка** — строки заголовка сдвинуты по горизонтали (первая слева, вторая с отступом, как «REDEFINE MODERN / VISUAL CULTURE»).
4. **✱** — красная звёздочка слева от заголовка секции (SVG, медленно вращается 20s).

---

## 4. Сетка, ритм, формы

- 12 колонок, контейнер `max-width: 1440px`, поля `clamp(16px, 4vw, 56px)`.
- Вертикальный ритм между секциями `clamp(96px, 14vh, 180px)`.
- Радиусы: карточки 20–24px, фото 16–20px, кнопки/теги 999px. **Светлые «экраны» и hero — не скругляются, а режутся `.panel-cut`** (см. §7).
- Бордеры 1px (`--line-light` / `--line-dark`). Тени — только мягкие направленные, без «свечения вокруг всего».
- Каждая секция — **своя композиция** (bento, сплит, горизонтальный скролл, стат-строка, аккордеон). Запрещено повторять «заголовок + 3 одинаковые карточки» по всей странице.

---

## 5. Liquid Glass (жидкое стекло)

Используем **точечно**, только поверх фото или цветного градиента (на плоском фоне стекло не видно и выглядит как слоп):

1. Плавающая навигация-пилюля (fixed, сверху по центру).
2. Мини-карточки поверх hero-фото (как «85%» в реф. 1): «6 месяцев до премьеры», «10+ фильмов о любви».
3. Кнопка «Смотреть все фото» поверх обложки пары.
4. Панель управления в лайтбоксе галереи (стрелки, счётчик кадров, закрыть).
5. Плашка цены поверх фото в блоке «Стоимость».

Рецепт:
```css
.glass {
  background: linear-gradient(135deg, rgba(255,255,255,.18), rgba(255,255,255,.05));
  backdrop-filter: blur(18px) saturate(170%);
  -webkit-backdrop-filter: blur(18px) saturate(170%);
  border: 1px solid rgba(255,255,255,.28);
  box-shadow: inset 0 1px 0 rgba(255,255,255,.45),   /* блик сверху */
              inset 0 -1px 0 rgba(255,255,255,.08),
              0 12px 32px -12px rgba(11,3,2,.55);
  border-radius: 999px; /* или 24px для карточек */
}
```
+ «преломление»: SVG-фильтр `feTurbulence` + `feDisplacementMap` (scale 20–40) через `backdrop-filter: url(#liquid)` — **только в Chromium** (проверка `CSS.supports`), в Safari/Firefox — обычный blur. Блик следует за курсором (radial-gradient по CSS-переменным `--mx/--my`).

---

## 6. Движение (motion)

Стек: **GSAP + ScrollTrigger** и **Lenis** (плавный скролл). Всё отключается при `prefers-reduced-motion: reduce`.

Сигнатурные моменты (не больше этих — остальное тихо):
1. **Интро «шторка»**: при загрузке красный бархат (hero-фото) раскрывается из щели по центру (clip-path), логотип проявляется, как титр. ≤ 1.6s, можно пропустить кликом.
2. **Таймкод**: в углу экрана фиксированный счётчик `00:00:00:00` в JetBrains Mono, крутится от прокрутки (scroll progress → время фильма). Линейка-засечки сверху (как в реф. 1) = прогресс.
3. **Заголовки**: построчное появление снизу с маской (`overflow:hidden` + `yPercent`), 0.9s, `power4.out`, stagger 0.08.
4. **Фото**: при входе в экран — `clip-path: inset()` раскрытие + лёгкий параллакс scale 1.12 → 1.
5. **Этапы работы**: закреплённая секция с горизонтальным скроллом «раскадровки».
6. **Финальные титры**: в футере контакты и реквизиты медленно «уезжают» вверх, как титры в кино (CSS-анимация, пауза при ховере).
7. Кнопки: hover — стрелка ↗ вылетает и возвращается, заливка «наливается» снизу. Магнитный эффект только для главной CTA.

Длительности: микро 150–220ms, появления 700–1000ms. Easing: `cubic-bezier(.2,.7,.1,1)`. Никаких bounce/elastic.

---

## 7. Компоненты

- **`.panel-cut` — панель со срезанными углами** (приём из реф. 3). Общий стиль всех светлых «экранов» и hero, чтобы страница выглядела цельно. Делается через `clip-path: polygon(...)`, а не `border-radius`:
  - углы срезаны под 45° на `--cut: clamp(28px, 2.6vw, 40px)`;
  - по центру верхнего и нижнего края — трапециевидный вырез шириной `--notch-w: clamp(120px, 22%, 320px)` и глубиной `--notch-d: clamp(10px, 1.1vw, 16px)`; соседние панели стоят через зазор 16px, и вырезы смотрят друг на друга;
  - `.panel-cut--plain` — только срезанные углы, без вырезов: для hero, где по центру сверху висит навигация;
  - внутри панели контент держится дальше `--notch-d` от края (по вертикали хватает обычных полей секции);
  - тени у панели не работают (clip-path их срезает) — глубину дают фото и контраст с `--night`.
- **Кнопка «Написать нам» (`.cta`)**: фон `--red-brand`, текст `--paper` (4.97:1), стрелка ↗. Hover: заливка `#B71F11` наливается снизу вверх за 220ms, стрелка уезжает вправо-вверх и возвращается новой снизу-слева, кнопка приподнимается на 1px; нажатие — `scale(.97)`. Easing `cubic-bezier(.2,.7,.1,1)`, без свечения. Размеры: `.cta--sm` 40px (шапка), `.cta--lg` 60px (футер).
- **Pill-кнопка** (вторичная): прозрачная, обводка 1px, как «EXPLORE NOW ↗» в реф. 1.
- **Тег**: mono 11px, обводка 1px, radius 999px («КАДР 007», «ПЕРМЬ», «2025»).
- **Eyebrow-строка** (из реф. 3): `СЦЕНАРИЙ ——— ПРОДАКШН ——— ФИНАЛЬНЫЕ ТИТРЫ` над крупным заголовком.
- **Стат-строка** (из реф. 3): 3–4 числа с тонкими вертикальными разделителями.
- **Карточка отзыва** (из реф. 2): тёмная `--wine` плашка, текст, внизу имя + кружок с инициалами (градиент red→ember).
- **Аккордеон FAQ**: номер в mono, вопрос крупно, «+» поворачивается в «×».
- **Лайтбокс галереи**: полноэкранный чёрный, фото по центру, снизу лента-миниатюр как киноплёнка (перфорация по краям — CSS), стрелки/свайп, счётчик `КАДР 04 / 17`.

---

## 8. Анти-слоп (проверять перед сдачей каждого блока)

Нельзя: AI-beige, фиолетово-синие градиенты, эмодзи в интерфейсе, курсивные serif-цитаты, «карточка в карточке в карточке», одинаковые карточки везде, цветная полоска-бордер сбоку карточки, пульсирующие точки, центрированный текст в длинных абзацах, стоковые иконки-линии в кружках, текст поверх фото без затемнения, placeholder-тексты и lorem ipsum.
Проверять: `/impeccable audit` и `/impeccable critique` после каждого крупного этапа.
